#!/bin/bash

set -eu

# no-op in this fork: dosfstools/e2fsprogs/btrfs-progs are declared via
# this image's own mmdebstrap.packages instead (see image.yaml), so they
# go through the same bulk essential-phase apt install as everything
# else, rather than this script's own separate late-customize-phase
# `chroot $1 apt install`, which failed to resolve the arm64 dynamic
# linker under cross-arch qemu emulation (amd64 host, arm64 target).
exit 0
